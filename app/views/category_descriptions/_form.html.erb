<%= form_for(@category_description) do |f| %>
  <% if @category_description.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@category_description.errors.count, "error") %> prohibited this category_description from being saved:</h2>

      <ul>
      <% @category_description.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
